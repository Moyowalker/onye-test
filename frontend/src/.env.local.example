# Auth0 Configuration for OAuth 2.0 + SMART on FHIR
# These values come from your Auth0 Application settings

# NextAuth.js requires these for security
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# Auth0 Application Configuration
# You'll get these when you create an Auth0 Application (Single Page Application type)
AUTH0_DOMAIN=https://your-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Auth0 API Identifier (for SMART scopes)
# This tells Auth0 which API we're requesting access to
AUTH0_AUDIENCE=https://your-fhir-api

# Backend API URL - where our FastAPI server runs
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: Enable debug mode for authentication troubleshooting
NEXTAUTH_DEBUG=false

# Optional: Auth0 connection strategy (leave empty for default)
# AUTH0_CONNECTION=Username-Password-Authentication